# Dependencies for the Vercel Python serverless function (api/report.py).
# Kept minimal: the hypersignal engine core does not import FastAPI/typer.
httpx>=0.27
pydantic>=2.6
eth-utils>=4.1
eth-hash[pycryptodome]>=0.7
