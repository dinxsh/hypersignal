# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/

# Env / secrets
.env
*.local

# Node / Vite
node_modules/
dist/
*.tsbuildinfo
# tsc -b composite emit from vite.config.ts
dashboard/vite.config.js
dashboard/vite.config.d.ts
# ...but DO commit the built dashboard: Vercel's Python builder serves it as
# static files (it can't run npm), so the single-project deploy needs it.
!dashboard/dist/
!dashboard/dist/**

# Scratch (live API response captures)
_live/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
.gstack/
